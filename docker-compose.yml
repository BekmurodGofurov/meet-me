services:
  backend:
    build: ./backend
    restart: unless-stopped
    ports:
      # Bound to loopback only - nginx on the same host proxies to it, so it
      # never needs to be reachable directly from outside this server.
      - "127.0.0.1:${BACKEND_PORT:-8090}:8080"
