# Copy this file to .env and set your real domain (must point at this
# server's IP in DNS - Let's Encrypt cannot issue a certificate for a bare
# IP address). For a quick IP-only test, see the note in Caddyfile instead.
DOMAIN=example.com
